{
  "private": true,
  "scripts": {
    "build": "vue-tsc --noEmit && nuxt build",
    "dev": "nuxt dev --port=3001",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.3",
    "nuxt-windicss": "^2.3.1",
    "vue-tsc": "^0.34.12"
  },
  "dependencies": {
    "@picocss/pico": "^1.5.0",
    "sass": "^1.51.0"
  }
}
